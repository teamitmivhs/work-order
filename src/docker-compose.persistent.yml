services:

  # ─── Database MySQL (persistent ke host path) ──────────────────────────────
  db:
    image: mysql:8.0
    container_name: work-order-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MYSQL_DATABASE:      ${DB_NAME:-dbwoit}
      MYSQL_USER:          ${DB_USER}
      MYSQL_PASSWORD:      ${DB_PASSWORD}
    volumes:
      # Data disimpan langsung di path host (bukan named volume)
      - /docker/work-order/mysql-data:/var/lib/mysql
      - ./db:/docker-entrypoint-initdb.d:ro
    networks:
      - workorder-net
    healthcheck:
      # FIX: pakai variable, bukan credential hardcode
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost",
             "-u", "${DB_USER}", "-p${DB_PASSWORD}"]
      interval: 10s
      timeout: 20s
      retries: 10
      start_period: 30s

  # ─── Rust Time Tracker ─────────────────────────────────────────────────────
  time-tracker:
    build:
      context: ./rust-engine
      dockerfile: Dockerfile
    container_name: work-order-time-tracker
    restart: unless-stopped
    environment:
      PORT: "9000"
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
    expose:
      - "9000"
    networks:
      - workorder-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ─── Go Backend ────────────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: work-order-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      time-tracker:
        # FIX: tunggu time-tracker healthy (sebelumnya service_started)
        condition: service_healthy
    env_file: .env
    environment:
      DB_HOST:          db
      DB_PORT:          "3306"
      DB_USER:          ${DB_USER}
      DB_PASSWORD:      ${DB_PASSWORD}
      DB_NAME:          ${DB_NAME:-dbwoit}
      JWT_SECRET:       ${JWT_SECRET}
      TIME_TRACKER_URL: "http://time-tracker:9000"
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
    expose:
      - "8080"
    networks:
      - workorder-net
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider",
             "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ─── Nginx ─────────────────────────────────────────────────────────────────
  nginx:
    image: nginx:1.25-alpine
    container_name: work-order-nginx
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "4323:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./login.html:/usr/share/nginx/html/login.html:ro
      - ./register.html:/usr/share/nginx/html/register.html:ro
      - ./summary.html:/usr/share/nginx/html/summary.html:ro
      - ./kaizen.html:/usr/share/nginx/html/kaizen.html:ro
      - ./techguide.html:/usr/share/nginx/html/techguide.html:ro
      - ./static:/usr/share/nginx/html/static:ro
    networks:
      - workorder-net

networks:
  workorder-net:
    driver: bridge